import React from "react";
import classes from "./OrderButton.module.css";

const OrderButton = () => {
  // Logic for order button component

  return (
    <button className={classes.orderButton}>
      Order
    </button>
  );
};

export default OrderButton;
